group = "org.example"
version = "1.0-SNAPSHOT"

buildscript {
    repositories {
        maven {
            url("${repoUrl}")
        }
    }
}

subprojects {
    repositories {
        maven {
            url("${repoUrl}")
        }
    }
}
